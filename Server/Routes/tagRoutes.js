const express = require('express');
const router = express.Router();

const tagController = require('../Controllers/tagController');

// get req: get tag and all associated files

// post request create tag, conditional if tag created on it's own or with the upload of a new file?

// patch request change tag name, location?, color?

// delete req delete tag

// Export router
module.exports = router;