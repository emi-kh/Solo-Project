const tagController = {};

// get middleware: 
// get all tags
// get specific tag and all associated files

// post middleware:
// create new tag

// patch middleware:
// change name, change color
// middleware for if file deletes or adds a tag?
// if collection deletes or adds a tag?

// delete middleware:
// delete tag from database and all associated files and collections

// Export controller
module.exports = tagController;